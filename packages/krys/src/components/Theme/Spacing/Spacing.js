import React from 'react'

import { Box } from '../../common'
import { H2, Section } from '../styled'

const Spacing = ({ breakpoints }) => (
  <Section>
    <Box margin='0 0 0 0.5rem'>
      <H2>Spacing</H2>
    </Box>
  </Section>
)

export default Spacing
